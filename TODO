* bar system-tray issues
* better nvim/vim/nvi integration

restructure {proper modules}:

configuration:
  root:
    headless?
      desktop
        sddm
        bspwm
        mate
          theme
        polybar
        rofi
        audio
          pulse
          jack
    shell
      zsh
      git
      tmux
      vim
    docker
hardware
networking

lifecycle:
  iso: install -> system: make-iso, update, drop, (auto-gc)
  repo: upgrade

vars:
  server?
  desktop?
    proaudio?
    touchscreen?
  docker?
  hostname
  username

